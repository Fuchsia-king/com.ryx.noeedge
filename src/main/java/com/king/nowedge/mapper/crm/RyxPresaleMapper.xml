<?xml version="1.0" encoding="UTF-8" ?>   
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">   
<mapper namespace="com.king.nowedge.mapper.crm.RyxPresaleMapper">    
    
    <sql id="select">		
		id,
	    contact,
	    phone,
	    icustomer,
	    cmobile,
	    biz_type bizType,
	    project,
	    latest,
	    oname,
	    source,
	    cdept,
	    cposition,
	    dept,
	    manager,
	    creater,
	    lcreate,
	    lmodified,
	    ideleted,
	    status,
	    district1,
	    district2,
	    district3,
	    email,
	    weixin,
	    qq,
	    addr,
	    postcode,
	    vdays,
	    descr      
	</sql>

	<sql id="where">
	
	    <where>	
			
			<if test="bizType != null and status != '' ">
				<![CDATA[ and biz_type = #{bizType} ]]>
			</if>
			
			<if test="project != null and project != '' ">
				<![CDATA[ and project = #{project} ]]>
			</if>
			
			<if test="source != null and source != '' ">
				<![CDATA[ and source = #{source} ]]>
			</if>
			
			<if test="creater != null and creater != '' ">
				<![CDATA[ and creater = #{creater} ]]>
			</if>
			
			<if test="dept != null and dept != '' ">
				<![CDATA[ and dept = #{dept} ]]>
			</if>
			
			<if test="status != null and status != '' ">
				<![CDATA[ and status = #{status} ]]>
			</if>
			
			<if test="manager != null and manager != '' ">
				<![CDATA[ and manager = #{manager} ]]>
			</if>
			
			<if test="icustomer != null ">
				<![CDATA[ and icustomer = #{icustomer} ]]>
			</if>
			
			<if test="keyword != null and keyword != '' ">
				<![CDATA[and ( oname like concat('%', #{keyword}, '%') or contact like concat('%', #{keyword}, '%') ) ]]>
			</if>
			
			
		</where>
	</sql>
   
    <insert id="create" parameterType="RyxPresaleDTO">   
    
       insert into ryx_pre_sale(
       
		    contact,
		    cmobile,
		    phone,
		    biz_type,
		    project,
		    latest,
		    oname,
		    source,
		    cdept,
		    cposition,
		    dept,
		    manager,
		    creater,
		    lcreate,
		    lmodified,
		    ideleted,
		    status,
		    district1,
		    district2,
		    district3,
		    email,
		    weixin,
		    qq,
		    addr,
		    postcode,
		    descr,
		    vdays,
		    icustomer
    
       ) 
       values
       (
       		#{contact},
		    #{cmobile},
		    #{phone},
		    #{bizType},
		    #{project},
		    #{latest},
		    #{oname},
		    #{source},
		    #{cdept},
		    #{cposition},
		    #{dept},
		    #{manager},
		    #{creater},
		     UNIX_TIMESTAMP(now()) ,
		     UNIX_TIMESTAMP(now()) ,
		    #{ideleted},
		    #{status},
		    #{district1},
		    #{district2},
		    #{district3},
		    #{email},
		    #{weixin},
		    #{qq},
		    #{addr},
		    #{postcode},
		    #{descr},
		    #{vdays},
		    #{icustomer}
       )        
       <selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
			select LAST_INSERT_ID() 
		</selectKey>
    </insert>
      
    
    
    <select id="queryUserId" resultType="java.lang.Long" parameterType="RyxPresaleQuery">
         
        select id from ryx_pre_sale         
        <include refid="where"/>		
		<if test="orderBy != null and orderBy !=''">
			order by ${orderBy}	
			<if test="sooort != null and sooort !=''">
				${sooort}
			</if>	
		</if>
		
		<if test="pageSize != null">
			limit ${startRow}, ${pageSize}
		</if>		
		
    </select>
    
    
    <select id="query" resultType="RyxPresaleDTO" parameterType="RyxPresaleQuery">
         
        select
         
        <include refid="select"/>
        
        from ryx_pre_sale 
        
        <include refid="where"/>
		
		<if test="orderBy != null and orderBy !=''">
			order by ${orderBy}	
			<if test="sooort != null and sooort !=''">
				${sooort}
			</if>	
		</if>
		
		<if test="pageSize != null">
			limit ${startRow}, ${pageSize}
		</if>		
		
    </select>
    
	
    
    <select id="countQuery" resultType="java.lang.Integer" parameterType="RyxPresaleQuery">   
        select count(1) from ryx_pre_sale 
        <include refid="where"/>  limit 1 ;
    </select>
    
    
    
    <select id="getById" resultType="RyxPresaleDTO" parameterType="java.lang.Long">   
        select 
        <include refid="select"/>
        from ryx_pre_sale where id = #{id}  limit 1;
    </select>    
    
    
     
    
    
    <update id="update" parameterType="RyxPresaleDTO">
    
        update ryx_pre_sale
         
        <set>	
					
			<if test="contact != null">
				<![CDATA[ contact = #{contact} ]]>,
			</if>	
			<if test="phone != null">
				<![CDATA[ phone = #{phone} ]]>,
			</if>		
			<if test="cmobile != null">
				<![CDATA[ cmobile = #{cmobile} ]]>,
			</if>		
			<if test="bizType != null">
				<![CDATA[ biz_type = #{bizType} ]]>,
			</if>
			<if test="project != null">
				<![CDATA[ project = #{project} ]]>,
			</if>		
			<if test="oname != null">
				<![CDATA[ oname = #{oname} ]]>,
			</if>
			<if test="source != null">
				<![CDATA[ source = #{source} ]]>,
			</if>
			<if test="cdept != null">
				<![CDATA[ cdept = #{cdept} ]]>,
			</if>
			<if test="vdays != null">
				<![CDATA[ vdays = #{vdays} ]]>,
			</if>
			<if test="cposition != null">
				<![CDATA[ cposition = #{cposition} ]]>,
			</if>
			<if test="dept != null">
				<![CDATA[ dept = #{dept} ]]>,
			</if>
			<if test="manager != null">
				<![CDATA[ manager = #{manager} ]]>,
			</if>
			
			<if test="ideleted != null">
				<![CDATA[ ideleted = #{ideleted} ]]>,
			</if>
			
			
			<if test="status != null">
				<![CDATA[ status = #{status} ]]>,
			</if>
			
			<if test="icustomer != null">
				<![CDATA[ icustomer = #{icustomer} ]]>,
			</if>
			
			<if test="district1 != null">
				<![CDATA[ district1 = #{district1} ]]>,
			</if>
			
			<if test="district2 != null">
				<![CDATA[ district2 = #{district2} ]]>,
			</if>
			
			<if test="district3 != null">
				<![CDATA[ district3 = #{district3} ]]>,
			</if>
			
			<if test="email != null">
				<![CDATA[ email = #{email} ]]>,
			</if>
			
			<if test="weixin != null">
				<![CDATA[ weixin = #{weixin} ]]>,
			</if>
			
			<if test="qq != null">
				<![CDATA[ qq = #{qq} ]]>,
			</if>
			
			<if test="addr != null">
				<![CDATA[ addr = #{addr} ]]>,
			</if>
			
			<if test="postcode != null">
				<![CDATA[ postcode = #{postcode} ]]>,
			</if>
			
			<if test="creater != null">
				<![CDATA[ creater = #{creater} ]]>,
			</if>
			
			<if test="descr != null">
				<![CDATA[ descr = #{descr} ]]>,
			</if>
			
			<if test="latest != null">
				<![CDATA[ latest = #{latest} ]]>,
			</if>
			
			lmodified = UNIX_TIMESTAMP(now())
		 </set>
		 
		 where	id = #{id}
		 	
    </update> 
   
    <delete id="delete" parameterType="java.lang.String">    
        delete from ryx_pre_sale where id = #{id}		 	
    </delete> 
    
</mapper> 